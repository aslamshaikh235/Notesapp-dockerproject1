apiVersion: apps/v1
kind: Deployment

metadata:
  name: notes-app-deployment

spec:
  replicas: 2

  selector:
    matchLabels:
      app: notes-app

  template:
    metadata:
      labels:
        app: notes-app

    spec:
      containers:
      - name: notes-app
        image: YOUR_ECR_IMAGE_URI:latest

        ports:
        - containerPort: 8000
